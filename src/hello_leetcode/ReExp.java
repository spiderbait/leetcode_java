package hello_leetcode;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class ReExp {
    public void execute(String str) {

        String regEx = "";
        Pattern p = Pattern.compile(regEx);
        Matcher m = p.matcher(str);
        if (m.find()) {

        }
    }
}
